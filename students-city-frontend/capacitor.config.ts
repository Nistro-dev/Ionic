import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.studentscity.app',
  appName: 'students-city-frontend',
  webDir: 'dist'
};

export default config;
